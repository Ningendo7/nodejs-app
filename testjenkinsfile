pipeline{
         agent any

         stages{

           stage('Dep install'){
                  agent { docker{ image'nodejs'
                  args '-u root:root'}}
                  steps{
                          
                         //  sh 'sudo chown -R 995:993 "/.npm"'
                           sh 'sudo npm install'
                  }
           }
         }
}